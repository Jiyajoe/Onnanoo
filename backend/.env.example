# No secrets needed for the prototype - the backend runs fully locally
# and does not call any external APIs. This file is kept for parity with
# the frontend's .env.example and as a place to add config later
# (e.g. MAX_OBJECT_COUNT, CORS origins) without touching code.

MAX_OBJECT_COUNT=20
